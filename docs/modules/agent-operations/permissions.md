# Agent Operations: Permissions

> Evidence status: Confirmed from code.

Both pages require an authenticated tenant membership and the existing `ASSISTANT` module entitlement. The feature contains no mutation path.

Requires owner confirmation: whether Agent Operations should later be limited to administrators and managers instead of following the existing Company Assistant role/module policy.
